# Suite integral (41 tests)

Corre contra un servidor local con Playwright:

```bash
python3 -m http.server 8124 &            # desde la raíz del repo
cd tests && npm init -y && npm i playwright
node suite.js
```

- **T1** Herramienta personal: sumas exactas, chip 5%, ayuda invisible tras recarga, guardar/comparar mes.
- **T2** Trimestre completo con **scores exactos calculados a mano** (LOS CAPOS = 31.775, TIBURONES = −11.950): trampa, eventos, austeridad, fiado, retomar tras recarga, proyector tardío, guards, reveal y análisis.
- **T3** Revancha limpia. **T4** Timeout real de 60s ("Juan decidió solo", sin puntos) + única máquina. **T5** Estáticos.

⚠️ Los scores esperados dependen de TODAS las constantes de la economía (ptsDest, trampas −1500, eventos, shocks, bonus). Si tocás una, recalculá a mano los esperados antes de culpar al código.
